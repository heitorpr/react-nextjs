# Locust Configuration File for Permission-Based API Testing

# Web UI Configuration
web-host = 0.0.0.0
web-port = 8089

# Test Configuration
host = http://localhost:8000
users = 10
spawn-rate = 2
run-time = 60s

# Logging
loglevel = INFO
logfile = locust.log

# HTML Report
html = reports/locust_report.html

# CSV Reports
csv = reports/locust_stats

# Headless mode (uncomment to run without web UI)
# headless = true
